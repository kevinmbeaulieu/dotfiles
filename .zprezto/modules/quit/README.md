To use, run commands of the form: `quit <application-name>`

Examples:

`quit Safari`

`quit Messages`


To quit multiple applications, specify them as separate arguments:
(e.g., `quit Safari Calendar iTerm`)
